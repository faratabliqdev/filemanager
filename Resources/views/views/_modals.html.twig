<div class="modal fade" id="js-confirm-delete" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">{{ 'title.delete'|trans }}</h4>
            </div>
            <div class="modal-body">
                {{ 'confirm.delete'|trans }}
            </div>
            <div class="modal-footer">
                {{ form_start(formDelete, {"attr" : {'class':'pull-right'}}) }}
                {{ form_widget(formDelete) }}
                {{ form_end(formDelete) }}
                <button type="button" class="btn btn-default pull-left" title="{{ 'button.cancel'|trans }}"
                        data-dismiss="modal"> {{ 'button.cancel'|trans }}</button>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="js-confirm-rename" style="overflow: hidden !important;" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            {#{{ form_start(formRename) }}#}
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                {#<h4 class="modal-title">{{ 'title.rename.file'|trans }}</h4>#}
            </div>
            <div class="modal-body" id="responceAjax">
                {#{{ form_row(formRename.name) }}#}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default pull-left"
                        data-dismiss="modal">{{ 'button.cancel'|trans }}
                </button>
                {#{{ form_row(formRename.send) }}#}
            </div>
            {#{{ form_end(formRename) }}#}
        </div>
    </div>
</div>
<div class="modal fade" id="addFolder" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            {{ form_start(form) }}
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">{{ 'title.add.folder'|trans }}</h4>
            </div>
            <div class="modal-body">
                {{ form_row(form.name) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default pull-left"
                        data-dismiss="modal">{{ 'button.cancel'|trans }}
                </button>
                {{ form_row(form.send) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
<div class="modal fade" id="js-display-image" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            {{ form_start(mediaform) }}
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">{{ 'button.add.files'|trans }}</h4>
            </div>
            <div class="modal-body">
                <div id="addFileMessage">
                </div>

                {{ form_widget(mediaform) }}

                <img src="" class="img-responsive" alt="">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default pull-left"
                        data-dismiss="modal">{{ 'button.cancel'|trans }}
                </button>
                {#{{ form_row(form.send) }}#}
                {#<button class="btn btn-success pull-right" id="fileupload">{{ button_label|default('Save') }}</button>#}
                <span class="btn btn-success btn-sm fileinput-button btn-action pull-right">
                    <i class="glyphicon glyphicon-file"></i>
                    <span>{{ 'button.add.files'|trans }}</span>
                    <input id="fileupload" type="file"
                           data-url="{{ path('file_manager_upload', fileManager.queryParameters) }}"
                           name="files[]" multiple="">
                           {#accept="{{ fileManager.configuration.accept is defined ? fileManager.configuration.accept : (fileManager.type ? accept(fileManager.type)) }}">#}
                </span>

            </div>
            {{ form_end(mediaform) }}
        </div>
    </div>
</div>
<div class="modal fade" id="addRemoteFile" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            {{ form_start(mediaRemoteForm) }}
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                            aria-hidden="true">&times;</span></button>
                <h4 class="modal-title">{{ 'button.add.files'|trans }}</h4>
            </div>
            <div class="modal-body">
            <div id="addRemoteFileMessage">
            </div>

                {{ form_widget(mediaRemoteForm) }}

                <img src="" class="img-responsive" alt="">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default pull-left"
                        data-dismiss="modal">{{ 'button.cancel'|trans }}
                </button>
                {#{{ form_row(form.send) }}#}
                {#<button class="btn btn-success pull-right" id="fileupload">{{ button_label|default('Save') }}</button>#}
                <button type="button" id="remoteFileupload" class="tn btn-success btn-sm btn-action pull-right" >
                    <i class="glyphicon glyphicon-file"></i>
                    {{ 'button.add.remote.files'|trans }}
                </button>
                {#<span class="btn btn-success btn-sm fileinput-button btn-action pull-right">#}
                    {#<i class="glyphicon glyphicon-file"></i>#}
                    {#<span>{{ 'button.add.remote.files'|trans }}</span>#}
                    {#<input id="fileupload" type="file"#}
                           {#data-url="{{ path('file_manager_remote_upload', fileManager.queryParameters) }}"#}
                           {#name="files[]" multiple="">#}
                    {#accept="{{ fileManager.configuration.accept is defined ? fileManager.configuration.accept : (fileManager.type ? accept(fileManager.type)) }}">#}
                {#</span>#}

            </div>
            {{ form_end(mediaRemoteForm) }}
        </div>
    </div>
</div>
{#<div class="modal fade" id="galleryList" tabindex="-1" role="dialog" aria-hidden="true">#}
    {#<div class="modal-dialog">#}
        {#<div class="modal-content">#}
            {#<div class="modal-header">#}
                {#<button type="button" class="close" data-dismiss="modal" aria-label="Close"><span#}
                            {#aria-hidden="true">&times;</span></button>#}
                {#<h4 class="modal-title">{{ 'button.list.gallery'|trans }}</h4>#}
            {#</div>#}
            {#<div class="modal-body">#}
                {#<div id="galleryList">#}
                {#</div>#}
                {#{% include '@AdsignFileManager/gallery/index2.html.twig' %}#}
                {#<img src="" class="img-responsive" alt="">#}
            {#</div>#}
            {#<div class="modal-footer">#}
                {#<button type="button" class="btn btn-default pull-left"#}
                        {#data-dismiss="modal">{{ 'button.cancel'|trans }}#}
                {#</button>#}
                {#{{ form_row(form.send) }}#}
                {#<button class="btn btn-success pull-right" id="fileupload">{{ button_label|default('Save') }}</button>#}
                {#<span class="btn btn-success btn-sm fileinput-button btn-action pull-right">#}
                    {#<i class="glyphicon glyphicon-file"></i>#}
                    {#<span>{{ 'button.add.files'|trans }}</span>#}
                    {#<input id="fileupload" type="file"#}
                           {#data-url="{{ path('file_manager_upload', fileManager.queryParameters) }}"#}
                           {#name="files[]" multiple="">#}
                    {#accept="{{ fileManager.configuration.accept is defined ? fileManager.configuration.accept : (fileManager.type ? accept(fileManager.type)) }}">#}
                {#</span>#}

            {#</div>#}
        {#</div>#}
    {#</div>#}
{#</div>#}

<script>
    function peditMedia(mediaId) {
        // $("#js-confirm-rename").modal("show");
        $('#responceAjax').addClass('alert alert-warning');
        $('#responceAjax').html('در حال ارسال فرم');
        console.log($('media_edit'));
        //alert($('.kazemi').val());
        var form_data = {
            id: mediaId,
            name: $('.kazemi').val(),
            post: true
        };
        console.log(form_data);
        $.ajax({
            url: editMediaUrl,
            data:  form_data,
            success:function(response) {
                $('#responceAjax').removeClass('alert alert-warning');
                $('#responceAjax').addClass('alert alert-success');
                // $("#responceAjax").html('');
                //
                // $('#addRemoteFileMessage').html(response['message']);
                // setTimeout(function() {$('#responceAjax').removeClass('alert alert-success');$('#js-confirm-rename').modal('hide');}, 4000);
                // $('#addRemoteFile').modal('hide');
                // location.reload();
            }
        });
    };
</script>